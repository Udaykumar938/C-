#include <iostream>
 using namespace std; 
 int main() 
 { 
 	double a, b, product; 
 	cout << "Enter two numbers: ";
   cin >> a >> b;   
   product = a * b; 
   cout << "Product of two given numbers  = " << product; 
   return 0; 
  }
